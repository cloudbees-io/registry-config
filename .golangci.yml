version: "2"

output:
  sort-order:
    - file
    - linter
  show-stats: true

linters:
  default: all
  enable:
    - wsl_v5
  disable:
    - wsl
    - gochecknoglobals
    - wrapcheck
    - depguard
    - exhaustruct
    - gochecknoinits
    - godox
    - mnd
    - err113
    - ireturn
    - gomoddirectives
    - tagliatelle
  
  settings:
    wsl_v5:
      allow-first-in-block: true
      allow-whole-block: false
      branch-max-lines: 2
    cyclop:
      max-complexity: 15
    gosec:
      excludes:
        - G204
        - G306
    varnamelen:
      max-distance: 10
      ignore-names:
        - ok
        - wf
        - ch
        - i
        - fn
      ignore-decls:
        - r interface{}
    perfsprint:
      strconcat: false
    nestif:
      min-complexity: 7
    funlen:
      ignore-comments: true
      lines: 65
    govet:
      enable:
        - shadow
  
  exclusions:
    paths:
      - ".*_test\\.go"
      - "fake_.*\\.go"
      - "fake/"
    generated: strict

formatters:
  default: all
  disable:
    - gci
    - gofumpt
